spring:
  application:
    name: Anti-Fake-DaybreakNews-Backend
  profiles:
    active: db

  datasource:
    url: jdbc:mysql://localhost:3307/daybreakdb?createDatabaseIfNotExist=true&autoReconnect=true&characterEncoding=UTF-8&allowMultiQueries=true&allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: password
  
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create-drop
  
  servlet:
    multipart:
      max-file-size: 10MB

supabase:
  url: https://gdvmhaboyscqbpiwggrn.supabase.co
  anon-key: ${SUPABASE_ANON_KEY}
  service-key: ${SUPABASE_SERVICE_KEY}
  bucket-name: ${SUPABASE_BUCKET_NAME:news-images}

server:
  port: 8080
